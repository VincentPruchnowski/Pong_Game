// Includes
#include <iostream>
#include <SFML/Graphics.hpp>
#include "input.h"

// Constante du programme
const int WIN_WIDTH = 800;
const int WIN_HEIGHT = 600;

// Namespaces
using namespace sf;
using namespace std;

// Prototypes - ex: Afficher le score des joueurs
void SetText(Text& txt, String str);
void CheckBtn();
